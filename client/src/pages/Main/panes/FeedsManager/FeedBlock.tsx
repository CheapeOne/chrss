import React from 'react';
import { Feed } from '#/types/api.types';

interface Props {
  feed: Feed;
}

const FeedBlock: React.FC<Props> = props => {
  return <div></div>;
};

export default FeedBlock;
